<!DOCTYPE html>
<html>
<head>
    <meta charset="utf-8">
    <title>Sanad Screening Results</title>
    <style>
        body { font-family: DejaVu Sans, sans-serif; font-size: 14px; color: #333; margin: 40px; }
        .header { text-align: center; margin-bottom: 30px; border-bottom: 2px solid #0d9488; padding-bottom: 15px; }
        .header h1 { color: #0d9488; margin: 0; font-size: 24px; }
        .header p { color: #666; margin: 5px 0 0; font-size: 12px; }
        .score-card { border: 1px solid #ddd; border-radius: 8px; padding: 15px; margin-bottom: 15px; }
        .score-card h3 { margin: 0 0 8px; color: #0d9488; }
        .score-value { font-size: 28px; font-weight: bold; color: #0d9488; }
        .severity { display: inline-block; padding: 4px 12px; border-radius: 12px; font-size: 12px; font-weight: bold; color: white; }
        .severity-minimal { background: #22c55e; }
        .severity-mild { background: #eab308; }
        .severity-moderate { background: #f97316; }
        .severity-moderately_severe { background: #ef4444; }
        .severity-severe { background: #b91c1c; }
        .section { margin-bottom: 20px; }
        .disclaimer { background: #f5f5f5; padding: 12px; border-radius: 6px; font-size: 11px; color: #666; margin-top: 30px; }
        .footer { text-align: center; margin-top: 30px; font-size: 10px; color: #999; }
    </style>
</head>
<body>
    <div class="header">
        <h1>Sanad</h1>
        <p>Mental Health Screening Results</p>
        <p>{{ $session->completed_at?->format('F j, Y') }}</p>
    </div>

    <div class="section">
        <div class="score-card">
            <h3>Depression Screening (PHQ-9)</h3>
            <span class="score-value">{{ $session->phq9_score }}</span> <span style="color: #999;">/ 27</span>
            <br>
            <span class="severity severity-{{ $phq9Severity }}">{{ ucfirst(str_replace('_', ' ', $phq9Severity)) }}</span>
            <p>{{ $phq9Description }}</p>
        </div>

        <div class="score-card">
            <h3>Anxiety Screening (GAD-7)</h3>
            <span class="score-value">{{ $session->gad7_score }}</span> <span style="color: #999;">/ 21</span>
            <br>
            <span class="severity severity-{{ $gad7Severity }}">{{ ucfirst(str_replace('_', ' ', $gad7Severity)) }}</span>
            <p>{{ $gad7Description }}</p>
        </div>
    </div>

    <div class="section">
        <h3>Overall Assessment</h3>
        <span class="severity severity-{{ $session->combined_severity }}">{{ ucfirst(str_replace('_', ' ', $session->combined_severity)) }}</span>
        <p>{{ $combinedDescription }}</p>
    </div>

    <div class="disclaimer">
        <strong>Important:</strong> This screening is not a diagnosis. It is a self-report tool designed to identify symptoms that may benefit from further evaluation. Please consult a qualified mental health professional for a comprehensive assessment and personalized treatment plan.
    </div>

    <div class="footer">
        <p>Generated by Sanad | Anonymous Screening | No personally identifiable information included</p>
    </div>
</body>
</html>
